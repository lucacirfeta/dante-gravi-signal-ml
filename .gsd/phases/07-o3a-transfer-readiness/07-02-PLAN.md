---
phase: 07-o3a-transfer-readiness
plan: 02
status: complete
---

# O3a approved-scope freeze plan

## Goal

Turn the four explicit author decisions into a reproducible, fail-closed O3a
preparation boundary without selecting windows, reading outcomes, or inventing
stage-level statistical parameters.

## Tasks

1. Preserve the unresolved decision gate and add a self-digested authorization
   record for the approved O3a-only scope, `CBC_CAT1`, fresh O3a artifacts, and
   diagnostic-only A2.
2. Freeze the current verified WSL/CUDA runtime and encoder provenance.
3. Bind the released O3b source representation for initial seeding only,
   explicitly reject O4a scientific imports, and install/verify the public
   reference bundle from reusable storage on `E:`.
4. Verify fail-closed behavior, exact hashes, runtime parity, and restored
   provenance tests; do not start DQ acquisition or scientific execution.

## Completion gate

The scope is approved, but `pipeline_execution_allowed` remains false until a
public O3a DQ snapshot and separate stage-specific population/statistical
contracts are frozen.
