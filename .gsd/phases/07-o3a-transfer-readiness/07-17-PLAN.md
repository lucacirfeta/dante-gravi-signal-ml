---
phase: 07-o3a-transfer-readiness
plan: 17
depends_on: [07-16]
status: in_progress
---

# O3a native classification

Authorized by the user's 2026-09-24 follow-up after verified thresholds.
Apply only the already-frozen stage-contract rule: detector-local score below
CI lower is BACKGROUND, above CI upper is ROBUST, otherwise AMBIGUOUS.
Equality at either endpoint is AMBIGUOUS. Do not change populations or scores.

1. Bind verified threshold/rescore receipts, source bytes, runtime and parent
   contracts. Test synthetic boundaries, detector locality, order/completeness,
   prior-label rejection, hash corruption and immutable deterministic replay.
   Preserve every source score-row field; add only native class and thresholds.
2. Run regression tests, freeze and commit the adapter before opening results.
3. Run CLASSIFY only after parent re-verification; independently replay-verify
   all output rows and counts. Report only verified counts, with no astrophysical
   or global-significance claim. Update STATE and summary, commit/push branch.

Keep taxonomy, coincidence, PEM and A2 promotion closed. Stop for any new
scientific/structural choice or provenance mismatch. Preserve all historical
artifacts and EOL qualifications; do not normalize frozen sources.
