---
phase: 07-o3a-transfer-readiness
plan: 14
wave: 1
depends_on: [07-13]
files_modified:
  - config/dante_o3a_native_calibration_cohort_v1.json
  - src/dante_light/o3a_native_calibration_cohort.py
  - scripts/freeze_dante_o3a_native_calibration_cohort.py
  - tests/test_dante_o3a_native_calibration_cohort.py
autonomous: false
must_haves:
  truths:
    - Exactly 5,000 native-calibration identities per detector are frozen before native scoring.
    - Selection reads only detector, GPS, candidate firewall, and source-provenance fields; no scores, classes, or taxonomy.
    - All selected 32 s windows have complete symmetric 4 s context and are guarded across detectors from candidates and every index-training window.
    - The 4 kHz O3a index and all historical 4 kHz runs remain immutable.
  artifacts:
    - A versioned native-calibration population contract and exact-identity ledger.
    - A PASS verifier binding the selected ledger to the complete primary-scan and index/cohort manifests.
---

# Plan 07.14: freeze O3a native-calibration identities

<objective>
Select a fresh detector-aware O3a-only background population for native-index
threshold calibration, before any native score or threshold calculation.
Keep the already verified 4 kHz primary scan, cohort, and index unchanged.
</objective>

<context>
- `config/dante_o3a_native_v1_stage_contract.json` fixes 5,000 rows/detector,
  block length 17, the 128 s cross-population start guard, and hash priority.
- `config/dante_o3a_initial_calibration_selector_v1.json` fixes a 295-stratum,
  hash-ranked complete-block selector **for the initial seed calibration only**.
- `config/dante_o4a_corrected_native_calibration_v2.json` fixes the historical
  native-calibration cardinality and cross-detector candidate/index guards,
  but uses an evenly spaced block selector rather than the O3a hash priority.
- `E:/dante_cache/dante_light/o3a_native_v1/primary_scan_8f0424e5f3ea2b94449eaddb0e1ccf61c5fd7ba91d54c839bfa89d0e26efad35/primary_scan.sqlite`
  is the frozen primary-scan identity/validated raw-frame ledger.
- `E:/dante_cache/dante_light/o3a_native_v1/native_cohort_876508390ba7f1eddf2c50342870e7e8880048b243f545487039974b51df9a2c/native_cohort.jsonl`
  is the complete manifest of index-training windows.
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Resolve the native-calibration block selector before implementation</name>
  <files>config/dante_o3a_native_calibration_cohort_v1.json</files>
  <action>
    Choose and freeze the mapping from eligible, guarded 64 s-grid windows to
    295 temporal blocks per detector. Recommended: partition chronologically
    into 295 nonempty strata and rank each stratum's complete 17-window blocks
    by SHA-256, following the approved O3a initial-calibration selector while
    applying the native candidate/index guards first. The final block supplies
    two point-estimate-only rows (5,000 total; 4,998 bootstrap rows). Do not
    silently copy the O4a evenly spaced selector or globally hash-rank blocks:
    either changes the native reference population.
  </action>
  <verify>The author explicitly confirms the selector rule in the contract.</verify>
  <done>The exact native population-selection algorithm is frozen before reading or selecting identities.</done>
</task>

<task type="auto">
  <name>Implement the frozen identity-only selection and verifier</name>
  <files>src/dante_light/o3a_native_calibration_cohort.py, scripts/freeze_dante_o3a_native_calibration_cohort.py, tests/test_dante_o3a_native_calibration_cohort.py</files>
  <action>
    Bind parents by hash and run key. Read only the primary-scan identity,
    image-hash, candidate-indicator and raw-frame columns, plus index/cohort
    detector-GPS identities. Apply inclusive cross-detector 128 s guards,
    require full 40 s context from the validated source-frame ledger, and
    freeze an atomic ledger. Verify exact identities, counts, source spans,
    nonoverlap and zero forbidden outcome fields. Do not score or open strain.
  </action>
  <verify>Run the focused WSL tests and the new ledger verifier; fail on a changed parent hash or one-byte ledger mutation.</verify>
  <done>5,000 H1 and 5,000 L1 identities, 4,998 complete-block bootstrap rows each, zero guard/context/provenance violations.</done>
</task>

</tasks>

<verification>
- [ ] Parent scan, cohort and index still independently verify.
- [ ] No native score, threshold or candidate class is computed in this stage.
- [ ] Failed and historical run directories are preserved.
</verification>

<success_criteria>
- [ ] The scientific selector decision is recorded before selection.
- [ ] The exact native-calibration identity ledger verifies PASS.
</success_criteria>

Future separate question, **not part of this checkpoint**: test a paired
O3a/O4a 16 kHz band extension for morphology-dependent sensitivity; neither
run may be changed unilaterally in the present comparison.
