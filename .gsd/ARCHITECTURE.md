# Architecture

> Auto-generated by /map on 2026-07-15

## Overview

Gravitational Wave Signal Machine Learning (dante-gravi-signal-ml). 
Analyzes LIGO/Virgo GW data using ML models (DINOv2) for glitch detection, classification, and signal extraction. The system uses a Domain Shift Defense mechanism and multiple pipelines.

```
┌─────────────────────────────────────────┐
│     [Entry Points: main.py, gui.py]     │
├─────────────────────────────────────────┤
│        [Pipelines (v1, v2, v3)]         │
│  (Data aggregation, physics/PEM veto)   │
├─────────────────────────────────────────┤
│            [Core ML Layer]              │
│ (DINOv2, Patch Producer, Score, Index)  │
├─────────────────────────────────────────┤
│             [Data Layer]                │
│       (GWOSC, nds2, local HDF5)         │
└─────────────────────────────────────────┘
```

## Components

### Core (`src/core/`)
- **data_loader.py:** GW data loading via GWpy/h5py.
- **encoder.py:** Torch/DINOv2 embedding layer.
- **patch_producer.py & patch_scorer.py:** Spectrogram patch extraction and scoring.
- **reference_index_builder.py:** Builds reference indexing for ML search.
- **injection.py:** Signal injection testing.

### Production Pipeline (`src/pipeline_v2_production/`)
- **build_native_index.py & production_cluster.py:** Clustering (HDBSCAN/UMAP).
- **physics_correlation.py & cross_detector_veto.py:** Multi-detector coincidence and physics vetoes.
- **pem_*.py:** PEM (Physical Environment Monitor) coherence and calibration tools.
- **poisson_upper_limit.py:** Statistical significance and FAR limits.
- **aggregate_report.py & production_writer.py:** Results aggregation and saving.

### Entry Points
- **main.py:** Core CLI entry.
- **gui.py:** Optional Graphical Interface.
- **live_monitor.py:** Real-time run monitoring.

## Data Flow

1. Data acquired via `gwpy`/`gwosc` or local HDF5 datasets.
2. Spectrogram generation and patching (`patch_producer.py`).
3. Core ML inference via `encoder.py` (DINOv2 embeddings).
4. Distance calculation against reference index (`patch_scorer.py`).
5. Vetoing and Physical correlation (cross-detector, PEM coherence).
6. Result clustering and statistical reporting.

## Technical Debt
- [ ] V1 and V3 pipelines overlap or are in varying states of deprecation/incubation.
- [ ] TODO markers present in `src/**/*.py` (needs review).
- [ ] Optional dependencies (NDS2) require specific conda environments.
