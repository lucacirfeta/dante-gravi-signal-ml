# =============================================================================
# gravi-signal-ml — Dependencies
# =============================================================================
# Install: pip install -r requirements.txt

# --- GW data layer ---
gwpy>=3.0.13
gwosc>=0.7.1
h5py>=3.10
# h5py: keep for GWOSC data loading. Note: trainingsetv1d1.h5
# is incompatible with h5py>=3.12 on Python 3.13.
# Use tar.gz extraction instead (see reference_builder.py).
astropy>=6.0

# --- Signal processing ---
numpy>=1.26
scipy>=1.12

# --- Visualization ---
matplotlib>=3.8
seaborn>=0.13

# --- ML framework (Phase 2+) ---
torch>=2.1.0
torchvision>=0.16.0
# DINOv2 loaded via torch.hub — no pip package needed.
# First run downloads ~90MB to ~/.cache/torch/hub/

# --- Image processing ---
Pillow>=10.0.0
scikit-image>=0.21.0

# --- Dimensionality reduction & clustering (Phase 3) ---
umap-learn>=0.5.6
scikit-learn>=1.3.0     # required for sklearn.cluster.HDBSCAN

# --- Utilities ---
PyYAML>=6.0
tqdm>=4.66.0
python-dotenv>=1.0

# --- Database (Phase 3.1 Gravity Spy) ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pandas>=2.0.0

# --- Dev tooling ---
pytest>=8.0
ruff>=0.3
mypy>=1.8
pre-commit>=3.6

# --- Data download (Phase 3.3) ---
requests>=2.31.0

# --- GUI ---
Gooey>=1.2.0a0
